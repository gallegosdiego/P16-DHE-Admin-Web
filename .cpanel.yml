---
deployment:
  tasks:
    # Deploy consolidado: 3 tareas cortas compatibles con el task runner de cPanel.
    # Mismo patron que el ultimo deploy exitoso (c1b71ad).
    # Toda la logica de migraciones y reparaciones esta en deploy-cpanel-all.php.
    - /bin/mkdir -p /home/danheiex/api.danheiexpress.com/storage/logs
    - /bin/cp -R api/. /home/danheiex/api.danheiexpress.com/
    - cd /home/danheiex/api.danheiexpress.com && /usr/local/bin/php scripts/deploy-cpanel-all.php 2>&1

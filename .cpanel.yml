---
deployment:
  tasks:
    # Deploy manual desde cPanel.
    # El orquestador prepara y verifica primero el esquema operativo crítico.
    # Solo después copia el código nuevo y ejecuta las tareas de runtime.
    - /bin/mkdir -p /home/danheiex/api.danheiexpress.com
    - /bin/bash api/scripts/deploy-cpanel-release.sh /home/danheiex/api.danheiexpress.com 2>&1
